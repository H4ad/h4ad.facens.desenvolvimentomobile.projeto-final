import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.facens.highlance',
  appName: 'highlance',
  webDir: 'dist/app',
  bundledWebRuntime: false,
};

export default config;
